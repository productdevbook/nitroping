name: nitroping
description: Flutter SDK for NitroPing self-hosted push notification service. Supports iOS (APNs) and Android (FCM).
version: 0.0.1
homepage: https://github.com/productdevbook/nitroping
repository: https://github.com/productdevbook/nitroping
issue_tracker: https://github.com/productdevbook/nitroping/issues

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^15.2.4
  firebase_core: ^3.12.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
