{
  "name": "@nitroping/web",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "import": "./dist/index.js" } },
  "files": ["dist", "src", "README.md"],
  "scripts": { "typecheck": "bunx tsc -p tsconfig.json", "build": "bunx tsc -p tsconfig.build.json", "test": "bunx vitest run --passWithNoTests" },
  "dependencies": { "@nitroping/contracts": "^0.1.0" }
}
